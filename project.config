[access]
	inheritFrom = policy
[project]
	description = Parent code that holds the pom.xml to build all the repos (Casablanca)
[access "refs/*"]
	owner = group ldap/onap-gerrit-policy-parent-committers
